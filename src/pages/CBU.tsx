// ─── CBU page — wraps the existing CBUCalculator unchanged ───────────────────
import React from 'react';
import CBUCalculator from '../CBUCalculator';

export function CbuPage() {
  return <CBUCalculator />;
}
